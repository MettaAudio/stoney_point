<p id="notice"><%= notice %></p>

<p>
  <strong>First name:</strong>
  <%= @caddy.first_name %>
</p>

<p>
  <strong>Last name:</strong>
  <%= @caddy.last_name %>
</p>

<p>
  <strong>Phone:</strong>
  <%= @caddy.phone %>
</p>

<p>
  <strong>School:</strong>
  <%= @caddy.school %>
</p>

<%= link_to '<i class="icon-pencil"></i>'.html_safe, edit_caddy_path(@caddy), :class => 'btn btn-small' %>
<%= link_to '<i class="icon-list"></i>'.html_safe, caddies_path, :class => 'btn btn-small' %>
