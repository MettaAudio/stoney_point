.row
  .col-xs-12
    h1
      ' New Volunteer
.row
  .col-xs-12
    = form_for(@person_form, url: volunteers_url, method: :post, html: {class: 'form-vertical' }) do |f|
      = render partial: 'people/form', locals: {f: f}
      = render partial: "form", locals: { f: f, person_form: @person_form }
      = hidden_field_tag "return_to", "volunteer"
      form-actions
        = f.button "Save", :class => 'btn btn-primary btn-large'